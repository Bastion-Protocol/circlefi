{
  "name": "circlefi-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for CircleFi protocol",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ bastion-protocol/circlefi",
    "create-local": "graph create --node http://localhost:8020/ bastion-protocol/circlefi",
    "remove-local": "graph remove --node http://localhost:8020/ bastion-protocol/circlefi",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bastion-protocol/circlefi"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.57.0",
    "@graphprotocol/graph-ts": "0.31.0"
  }
}